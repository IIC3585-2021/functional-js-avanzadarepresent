const { player1, player2, simulation1, simulation2 } = require('./simulations');
const { playGame } = require('./flow');

// Simulation 1
// playGame(simulation1, player1, player2);

// Simulation 2
playGame(simulation2, player1, player2);
